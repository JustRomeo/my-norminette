/*
** Roméo's PROJECT, 2018
** NORMINETTE
** File description:
** Error Other
*/

#include "prototype.h"

int malloc_function(char **file)
{
    /* int c = 0; */
    /* int line = 1; */
    /* int nb_char = 0; */
    int nb_error = 0;

    /* while (file[c] != '\0') { */
    /*     if (file[c] == ' ' && file[c + 1] == 'm' && file[c + 2] == 'a' && */
    /*         file[c + 3] == 'l' && file[c + 4] == 'l' && file[c + 5] == 'o' && */
    /*         file[c + 6] == 'c') */
    /*         nb_error++; */
    /*     if (file[c] == '\n') */
    /*         line ++; */
    /*     c ++; */
    /* } */
    return (nb_error);
}

int open_function(char **file)
{
    //int c = 0;
    //int line = 1;
    //int nb_char = 0;
    int nb_error = 0;

    /*while (file[c] != '\0') {
      if (file[c] == ' ' && file[c + 1] == 'o' && file[c + 2] == 'p' && 
      file[c + 3] == 'e' && file[c + 4] == 'n' && file[c + 5] == '(') 
      nb_error++;
      if (file[c] == '\n') 
      line ++; 
      c ++; 
      } */
    return (nb_error);
}

int else_error(char **file, char *name)
{
    //printf("\n\e[35mLEAK MEMORY && DANGEROUS FUNCTIONS\t--------------\e[0m\n");
    //printf("/!\\");
    //if (lookingfor_open(file, name) != 0)
    //printf("\e[35m/!\\ NO CLOSE x %d /!\\\e[0m\n", lookingfor_open(file, name));
    //if (lookingfor_free(file, name) != 0)
    //printf("\e[35m/!\\ NO FREE x %d  /!\\\e[0m\n", lookingfor_free(file, name));
    return (0);
}
