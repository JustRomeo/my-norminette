#ifndef __Hpp__
#define __Hpp__

#include <vector>
#include <iostream>

using namespace std;
class Hpp {
    public:
        Hpp();
        ~Hpp();

        int norminette_hpp(vector<string> tab);

    private:
        int classe;
        bool shield;

    protected:
};

#endif