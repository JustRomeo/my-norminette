#ifndef __Errors__
#define __Errors__

#include <iostream>

using namespace std;
class Errors {
    public:
        Errors();
        ~Errors();

        void printer_error(string str, int line);

    private:
    protected:
};

#endif