#ifndef __ASM__
#define __ASM__

#include <vector>
#include <iostream>

using namespace std;
class ASM {
    public:
        ASM();
        ~ASM();

        int norminette_asm(vector<string> tab);

    private:
    protected:
};

#endif