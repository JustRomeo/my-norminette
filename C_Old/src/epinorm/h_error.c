/*
** EPITECH PROJECT, 2018
** NORMINETTE
** File description:
** error H
*/

#include "prototype.h"

static int H1error(char *file, int *major)
{
    int c = 0;
    int nb_char = 0;
    int nb_error = 0;

    return (nb_error);
}

static int H2error(char *file, int *minor)
{
    int c = 0;
    int nb_char = 0;
    int nb_error = 0;

    return (nb_error);
}

static int H3error(char *file, int *minor)
{
    int c = 0;
    int nb_char = 0;
    int nb_error = 0;

    return (nb_error);
}

int H_error(char **file, norm_s *fault, f_norm *norm, char *name)
{
    return (0);
}
